# 덕터스(Ductors) 인수인계 문서

> 최종 업데이트: 2026-06-21
> 환기·닥트 시공 견적 매칭 플랫폼

---

## 🔴 다음 세션에서 가장 먼저 풀 미해결 과제

### 1. [최우선] 고객 마이페이지에 "내 견적 요청"이 0건으로 나옴 (데이터는 멀쩡)
- **증상**: 고객1(local_customer1, userId=697)로 로그인 → 마이페이지 "내 견적 요청" 탭에 "아직 견적 요청이 없습니다" 표시
- **확인된 사실**:
  - DB에 견적 8건 전부 존재 (삭제 안 됨). 그중 697이 의뢰한 것 5건(id 4,5,6,7,8)
  - 697 계정: role=user, deletedAt=null, 정상. 중복 계정 없음(이메일 bill.ju1007@gmail.com 단일)
  - 코드 로직은 정상으로 보임: myQuotes(`server/routers.ts`) → getQuotesByCustomer(ctx.user.id), 마이페이지(`MyPage.tsx` 247번)
- **마지막 디버그 시도**: myQuotes에 `console.log`로 ctx.user.id/openId/조회건수 찍는 로그를 심었으나, 대표님이 서버 재시작 후 로그를 확인하기 전에 세션 종료. (그 디버그 로그는 이번에 원복함)
- **다음 액션**: myQuotes 디버그 로그를 다시 심고, 서버 터미널에서 `[myQuotes 디버그] user.id=?, openId=?, 조회=?건` 확인.
  - user.id가 697이 아니면 → **세션/로그인 매핑 문제** (자체로그인 시 ctx.user.id가 잘못 잡힘)
  - user.id=697인데 0건이면 → getQuotesByCustomer 쿼리 문제
  - 697인데 5건이면 → 프론트 캐시/렌더 문제
- **추가 단서**: 대표님 말씀 - "1.technicians를 customer1이 지정했는데 둘 다 대시보드에서 안 보임. customer1로 공개+지정한 것이 1.technicians 대시보드엔 여전히 보이는데 customer1 본인 마이페이지에선 지정도 공개도 다 사라짐" → customer1의 myQuotes만 빈 결과. 파트너(1.technicians=partnerId 1) 쪽은 정상 노출.
- **세션 종료 시점 첨부화면**: 고객1 마이페이지 "아직 견적 요청이 없습니다" + 서버 터미널 화면(디버그 로그 확인하려던 시점)

### 2. 첨부 사진 표시 - 수정했으나 미검증
- 견적 생성 시 attachments(사진) 전송 누락을 수정함(QuoteRequest.tsx에 `attachments,` 추가, 커밋 7b3de60)
- **단, 사진 첨부한 새 견적으로 파트너 상세에서 사진 보이는지 최종 확인 못 함**

### 3. 지정 견적 노출 - 데이터는 정상, 표시 미확인
- DB상 지정견적(id 5,6,8)은 designatedPartnerId=1로 정상 저장됨
- partnerId=1 파트너 대시보드 "지정 견적" 탭에 보여야 하나 최종 확인 못 함 (1번 과제와 연관 가능성)

---

---

## 1. 비즈니스 개요

- **서비스명**: 덕터스(Ductors) — 환기·닥트 시공 견적 매칭 플랫폼
- **운영사**: (주)멀티테크니션스 (팬직 자회사)
- **도메인**: ductors.co.kr
- **모델**: 고객(시공 의뢰) ↔ 파트너(시공업체) 견적 매칭. 파트너가 견적 열람 시 토큰/포인트 차감 (수익원)

### 사업자 정보
- 법인명: 주식회사 멀티테크니션스 / 대표: 김재유
- 사업자등록번호: 554-87-02355 / 법인등록번호: 131411-0507479
- 주소: 경기도 안산시 단원구 고잔로 55, B102호 (고잔동, 중앙오피스텔)
- 개업: 2021.10.15 / 업종: 전자상거래 소매중개업 등

---

## 2. 기술 환경

| 구분 | 내용 |
|---|---|
| 프론트엔드 | React 19 + TypeScript + Tailwind + wouter(라우팅) |
| 백엔드 | Express + tRPC |
| DB | MySQL 8.0 + Drizzle ORM |
| 패키지 매니저 | pnpm |
| 저장소 | github.com/unikor21-beep/ductors |
| 로컬 폴더 | C:\Users\JaeyU's\ductors |

### 작업 방식
- Claude가 코드 수정 → GitHub push
- 대표님이 `git pull origin main`으로 받아 로컬 테스트
- 소통: 쉬운 한국어, 한 단계씩, 스크린샷 해석

### .env 주요 값
```
DATABASE_URL="mysql://root:Multi@12345@localhost:3306/ductors"
JWT_SECRET="ductors-secret-key-2024"
NODE_ENV="development"
KAKAO_REST_API_KEY="..."
OWNER_OPEN_ID="kakao_4950078098"   (관리자 계정)
NTS_API_KEY="..."   (국세청 사업자번호 인증 API)
```

---

## 3. 자주 발생하는 환경 이슈 (중요)

| 증상 | 해결 |
|---|---|
| **포트 3001로 밀림** (카카오 로그인 깨짐) | `pnpm dev`는 하나만 실행. 3000 점유 시 `taskkill /F /IM node.exe` 후 재실행 |
| 변경사항이 화면에 안 보임 | **Ctrl+Shift+R** 강력 새로고침 |
| DB 스키마 변경 시 | 해당 setup 스크립트 실행 필요 (아래 참고) |
| 명령어를 .env/서버창에 잘못 입력 | 명령어는 **새 터미널**에서 (서버 창은 서버 전용) |
| 서버 로그 빨간 메시지 (VITE_ANALYTICS 등) | 무관, 무시 |

---

## 4. DB 셋업 스크립트 (scripts/)

새 기능 추가 시 해당 스크립트를 **한 번만** 실행:

| 스크립트 | 용도 |
|---|---|
| `seed-categories.mjs` | 카테고리 대분류/소분류 초기 등록 |
| `fix-sort-order.mjs` | 카테고리 순서값 초기화 |
| `add-business-license.mjs` | 사업자등록증 컬럼 추가 |
| `setup-nts-key.mjs` | 국세청 API 키를 .env에 추가 |
| `setup-wallet.mjs` | 지갑 테이블(토큰/포인트/거래내역/설정) 생성 |
| `setup-campaigns.mjs` | 신규가입 보너스 캠페인 테이블 생성 |

실행 예: `node scripts/setup-wallet.mjs`

---

## 5. 완료된 기능

### 카테고리 (2단계)
- 대분류 4개(식당/가정/공장/상업) + 소분류 9개
- 관리자 순서변경, 활성토글(임시숨김)/하드삭제(영구) 구분
- 공용 컴포넌트 `CategorySelect.tsx`

### 지역 선택 (2단계)
- 시/도 → 구/시/군, 전국 행정구역 (`RegionSelect.tsx`)
- 순서: 서울 → 광역시 → 도 → 세종/제주

### 견적 의뢰
- 카테고리/지역 2단계 선택, 사진 첨부(최대 3장), 필수값 검증
- 공개 견적 / 지정 견적(특정 파트너 지목) 구분

### 파트너
- `PartnerAvatar.tsx`: 로고 또는 회사명 첫글자 컬러 아바타
- 마이페이지: 파트너=정보수정 / 일반=견적목록
- 파트너는 견적의뢰·파트너찾기 차단 (흐리게+안내)
- **사업자등록증 첨부 필수**
- **국세청 사업자번호 인증** (진위/휴폐업 확인, NTS API)

### 회원가입/약관
- 단순 가입(고객/파트너 구분 제거), 카카오/네이버/구글 버튼
- 약관 페이지: `/terms`(일반), `/privacy`(개인정보), `/marketing`(마케팅), `/partner-terms`(파트너 전용)
- 파트너 등록 시 개인정보 중복동의 제거 (가입 시 이미 동의)
- 약관 임시값(사업자번호 등) 빨간색 표시 → 오픈 전 수정용

### 지갑 시스템 ⭐ (핵심 수익 구조)
- **토큰**: 유료 충전 사이버머니 (현재 관리자 수동충전, PG는 미연동)
- **포인트**: 프로모션 지급, 유효기간 있음(1/10/30일)
- **차감 순서**: 포인트 먼저 → 토큰 (FIFO로 만료 임박 포인트부터)
- 파트너 대시보드 "지갑" 탭: 잔액 + 거래내역 + 충전 안내
- 관리자 "지갑 관리" 탭:
  - 가격 설정 (지정 열람가 5만 / 공개 열람가 1만 / 월 구독료 5만 - 기본값)
  - 토큰 수동 충전 (개별)
  - **신규가입 자동 보너스 캠페인** (기간 설정 → 그 기간 승인 파트너에게 자동 지급)
  - **일괄 포인트 지급** (체크박스 다중선택 + 지역별/등급별 필터)
  - 파트너 잔액 현황

### 견적 리드 (파트너 대시보드)
- 지정 견적 / 공개 견적 하위 탭 분리
- 지정 견적은 강조 표시(테두리+뱃지) + 건수 뱃지

### 기타
- 브랜드 테마: 초록(oklch 135), `DuctorsLogo.tsx` 임시 텍스트 로고(#84cc16)
- 메인 배경 이미지 관리자 관리, 다음 주소 API, 구글 지도 연동, 파트너 등급 자동평가

---

## 6. 관리자 계정
- 카카오 openId: `kakao_4950078098` (role='admin')
- 관리자 대시보드: `/admin`
- 탭: 통계 / 회원 / 파트너 / 견적 / 리뷰 / 상품 / 카테고리 / **지갑 관리** / 배경 관리

---

## 7. 외부 API 연동 현황

| API | 용도 | 상태 |
|---|---|---|
| 카카오 로그인 | 소셜 로그인 | ✅ 연동됨 |
| 국세청 사업자번호 | 진위/휴폐업 확인 | ✅ 연동됨 (NTS_API_KEY) |
| 다음 주소 검색 | 우편번호/주소 | ✅ 연동됨 |
| 구글 지도 | 파트너 위치 표시 | ✅ 연동됨 |
| 네이버/구글 로그인 | 소셜 로그인 | ⏸️ 버튼만 (실연동 대기) |
| PG 결제 | 토큰 충전 | ❌ 미연동 (관리자 수동충전) |
| 카카오 알림톡 | 알림 발송 | ❌ 미연동 (todo 19단계) |

> ⚠️ **보안**: NTS_API_KEY가 개발 중 노출됨 → 정식 오픈 전 공공데이터포털에서 재발급 권장

---

## 8. 앞으로 할 일 (todo.md 참고)

### 18단계: 메인페이지 프로모션 배너
- 이미지 + 링크버튼, 버튼 9분할 위치(상중하×좌중우) 선택
- 활성/순서/기간한정 설정, 기간 지나면 자동 비노출

### 19단계: 카카오톡 알림 시스템
- **사전 준비(대표님)**: 비즈니스 채널 개설+인증 → 딜러사(비즈고 등) 계약 → 템플릿 심사
- 발송 시나리오: 견적 의뢰/제출, 가입완료, 파트너 승인 (정보성=알림톡)
- 공지/이벤트는 친구톡/브랜드메시지 (수신동의 필요)

### 기타 남은 일
- 매칭 알고리즘 (현재 getPublicQuotes는 필터 없이 전체 노출)
- 구독 시스템 (월정액 무제한 열람, 가격설정만 됨)
- 실제 사진 S3 업로드 (현재 base64 임시)
- 실제 결제 PG 연동
- AI 단계 (추천/생성형, llm.ts 자리 있음)
- 로고 원본파일 교체 (현재 임시 텍스트)

---

## 9. 매칭 알고리즘 로드맵 (4단계)
1. 규칙 기반 매칭 (지역+공사종류 필터)
2. 데이터 축적
3. 협업 필터링 AI
4. 생성형 AI (외부 API, llm.ts 자리 있음)
